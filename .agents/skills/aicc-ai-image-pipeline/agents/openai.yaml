interface:
  display_name: "AICC 이미지 파이프라인"
  short_description: "OpenAI·Gemini 이미지 생성 안전 기준"
  default_prompt: "Use $aicc-ai-image-pipeline to fix or extend CT_aicc image generation safely."
policy:
  allow_implicit_invocation: true
