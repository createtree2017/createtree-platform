# 메뉴관리시스템 설계서

## 개요
- **기능명**: 메뉴관리시스템
- **작성일**: 2026-02-20
- **Plan 참조**: `docs/01-plan/features/1-20260220-메뉴관리시스템_재편.plan.md`

---

## Phase 0: admin.tsx 컴포넌트 분리 (최우선)

### 분리 대상 및 새 파일 매핑

| admin.tsx 내 컴포넌트 | 줄 범위 | → 새 파일 (components/admin/) | 비고 |
|---------------------|--------|-------------------------------|------|
| `ImageGallery` + `ImageItem` | 194~493 | `ImageGalleryAdmin.tsx` | 인터페이스 포함 |
| `LanguageSettings` | 495~502 | `LanguageSettings.tsx` | 8줄 스텁 |
| `PersonaManager` | 978~1181 | `PersonaManager.tsx` (덮어쓰기) | 기존 빈 파일 대체 |
| `PersonaForm` + `PersonaFormProps` | 1182~1709 | `PersonaForm.tsx` | PersonaManager에서 import |
| `CategoryManager` | 1711~1851 | `PersonaCategoryManager.tsx` (덮어쓰기) | 채팅 카테고리용 |
| `CategoryForm` + `CategoryFormProps` | 1852~2037 | `PersonaCategoryForm.tsx` | CategoryManager에서 import |
| `ConceptCategoryManager` | 2038~2178 | `ConceptCategoryManagerInline.tsx` | 이름 충돌 방지 |
| `ConceptCategoryForm` + Props | 2179~2363 | `ConceptCategoryForm.tsx` | |
| `ConceptManager` | 2365~2686 | 기존 `ConceptManagement.tsx`와 공존 | admin.tsx에서만 사용하는 레거시 |
| `ConceptForm` + Props | 2687~3509 | `ConceptForm.tsx` | 814줄 |
| `VariableForm` + Props | 3510~3732 | `VariableForm.tsx` | ConceptForm에서 import |
| `ABTestManager` | 3734~3944 | `ABTestManager.tsx` | |
| `ABTestForm` + Props | 3945~4259 | `ABTestForm.tsx` | |
| `ImageTester` | 4261~4641 | `ImageTester.tsx` | Concept 인터페이스 포함 |
| `MusicStylePromptManager` | 4643~4795 | `MusicStylePromptManager.tsx` | |
| `ApplicationManagement` | 4797~5060 | `ApplicationManagement.tsx` | |

### 분리 원칙

1. **기능 변경 없이 파일 이동만** — 각 컴포넌트를 그대로 복사, export/import만 수정
2. **공유 스키마** — `personaFormSchema`, `categoryFormSchema`, `conceptCategorySchema`, `conceptSchema` 등 Zod 스키마는 해당 Form 컴포넌트 파일에 포함
3. **의존성 체인** — 분리 후 `admin.tsx`에서 모든 새 파일을 import하여 기존 JSX 렌더링 유지
4. **import 정리** — 분리 대상 컴포넌트가 사용하던 import 문을 해당 파일로 이동

### 분리 후 admin.tsx 구조 (~500줄)

```typescript
// admin.tsx (분리 후)
import 문들 (~60줄) // 27개 기존 + 16개 새 분리 파일

export default function AdminPage() {
  // 탭/서브탭 상태 관리 (~130줄)
  // JSX: 탭 렌더링 (~310줄)
}
```

---

## Phase 1: DB 스키마 변경

### 새 테이블: `main_menus`

```typescript
// shared/schema.ts — Drizzle ORM
export const mainMenus = pgTable("main_menus", {
  id: serial("id").primaryKey(),
  menuId: text("menu_id").notNull().unique(), // 'my-missions', 'culture-center', 'ai-create', 'gallery', 'my-page'
  title: text("title").notNull(),             // '나의미션', '문화센터', 'AI생성', '갤러리', '마이페이지'
  icon: text("icon").notNull(),               // Lucide 아이콘 이름: 'Trophy', 'Target', 'Sparkles', 'Images', 'User'
  path: text("path").notNull(),               // 기본 경로: '/mymissions', '/missions', '/', '/gallery', '/profile'
  
  // 홈 설정: 전용 홈 vs 하위메뉴 중 선택
  homeType: text("home_type").notNull().default("dedicated"), // 'dedicated' | 'submenu'
  homeSubmenuPath: text("home_submenu_path"),  // homeType='submenu'일 때 이동할 경로

  isActive: boolean("is_active").notNull().default(true),
  order: integer("order").notNull().default(0),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

### 기존 테이블 변경: `service_items`

```diff
 export const serviceItems = pgTable("service_items", {
   ...기존 필드,
+  mainMenuId: integer("main_menu_id").references(() => mainMenus.id),
 });
```

### 초기 seed 데이터

```typescript
const initialMenus = [
  { menuId: 'my-missions', title: '나의미션', icon: 'Trophy', path: '/mymissions', isActive: false, order: 0 },
  { menuId: 'culture-center', title: '문화센터', icon: 'Target', path: '/missions', isActive: true, order: 1 },
  { menuId: 'ai-create', title: 'AI 생성', icon: 'Sparkles', path: '/', isActive: true, order: 2 },
  { menuId: 'gallery', title: '갤러리', icon: 'Images', path: '/gallery', isActive: true, order: 3 },
  { menuId: 'my-page', title: 'MY', icon: 'User', path: '/profile', isActive: true, order: 4 },
];
```

### 관계 설정

```typescript
export const mainMenusRelations = relations(mainMenus, ({ many }) => ({
  serviceItems: many(serviceItems)
}));

// serviceItemsRelations 확장
export const serviceItemsRelations = relations(serviceItems, ({ one }) => ({
  category: one(serviceCategories, { ... }),
  mainMenu: one(mainMenus, {
    fields: [serviceItems.mainMenuId],
    references: [mainMenus.id]
  })
}));
```

---

## Phase 2: API 설계

### 엔드포인트

| Method | Path | 설명 | 인증 |
|--------|------|------|------|
| GET | `/api/main-menus` | 활성 메뉴 목록 (프론트용) | 불필요 |
| GET | `/api/admin/main-menus` | 전체 메뉴 목록 (관리자용) | 관리자 |
| POST | `/api/admin/main-menus` | 메뉴 생성 | 관리자 |
| PATCH | `/api/admin/main-menus/:id` | 메뉴 수정 (활성/비활성, 홈설정 등) | 관리자 |
| DELETE | `/api/admin/main-menus/:id` | 메뉴 삭제 | 관리자 |
| PATCH | `/api/admin/main-menus/reorder` | 메뉴 순서 변경 | 관리자 |

### 요청/응답 스키마

#### `GET /api/main-menus` (공개)
```json
// Response — 활성 메뉴만, order 순
[
  {
    "id": 3,
    "menuId": "ai-create",
    "title": "AI 생성",
    "icon": "Sparkles",
    "path": "/",
    "homeType": "dedicated",
    "homeSubmenuPath": null,
    "isActive": true,
    "order": 2
  }
]
```

#### `PATCH /api/admin/main-menus/:id`
```json
// Request — 부분 업데이트
{
  "isActive": false
}
// 또는
{
  "homeType": "submenu",
  "homeSubmenuPath": "/baby-face"
}
// 또는
{
  "title": "AI 스튜디오"
}
```

#### `PATCH /api/admin/main-menus/reorder`
```json
// Request
{
  "orders": [
    { "id": 1, "order": 0 },
    { "id": 3, "order": 1 },
    { "id": 2, "order": 2 }
  ]
}
```

---

## Phase 3: 관리자 UI 컴포넌트 구조

### 새 파일: `components/admin/MenuManagement.tsx`

메뉴관리 탭의 메인 컴포넌트. 5개 메인 메뉴를 아코디언 형태로 표시.

```
MenuManagement
├── 메인 메뉴 목록 (드래그 정렬 가능)
│   ├── [🟡 나의미션] ─ 활성/비활성 토글 ─ 홈설정 버튼
│   │   ├── 미션관리 패널 → <MissionManagement /> 렌더링
│   │   └── 하위메뉴관리 패널 → <ServiceItemManagement mainMenuId={1} />
│   ├── [🟢 문화센터] ─ 활성/비활성 토글 ─ 홈설정 버튼
│   │   └── 하위메뉴관리 패널
│   ├── [🔵 AI생성] ─ 활성/비활성 토글 ─ 홈설정 버튼
│   │   ├── 페이지관리 → 배너/메뉴카드/인기스타일/갤러리 서브탭
│   │   ├── 이미지생성관리 → <ConceptManagement /> 등
│   │   ├── 채팅시스템관리 → <PersonaManager /> 등
│   │   ├── 음악생성관리 → <MusicStylePromptManager />
│   │   ├── 포토북관리 → 포토북 서브탭
│   │   └── 하위메뉴관리 → <ServiceItemManagement mainMenuId={3} />
│   ├── [🟣 갤러리] ─ ...
│   └── [🟠 마이페이지] ─ ...
```

### admin.tsx 새 탭 구조

```typescript
// 변경 전
<TabsTrigger value="chat-menu">채팅 메뉴</TabsTrigger>
<TabsTrigger value="image-menu">이미지 생성</TabsTrigger>
<TabsTrigger value="music-prompts">음악 프롬프트</TabsTrigger>
<TabsTrigger value="milestones">마일스톤</TabsTrigger>
<TabsTrigger value="missions">미션 시스템</TabsTrigger>
<TabsTrigger value="photobook-management">포토북 관리</TabsTrigger>
<TabsTrigger value="ui-content">UI 컨텐츠 관리</TabsTrigger>
<TabsTrigger value="member-management">회원관리</TabsTrigger>
<TabsTrigger value="system-settings">시스템 설정</TabsTrigger>
<TabsTrigger value="languages">언어 설정</TabsTrigger>

// 변경 후
<TabsTrigger value="menu-management">메뉴관리</TabsTrigger>
<TabsTrigger value="member-management">회원관리</TabsTrigger>
<TabsTrigger value="system-settings">시스템 설정</TabsTrigger>
<TabsTrigger value="milestones">마일스톤</TabsTrigger>
<TabsTrigger value="languages">언어 설정</TabsTrigger>
```

### 홈 설정 UI

`MenuHomeSettingDialog.tsx` — 각 메인 메뉴의 홈 설정 다이얼로그

```
┌─────────────────────────────────────┐
│  🏠 AI생성 홈 설정                   │
│                                     │
│  ○ 전용 홈 페이지 사용 (/)           │
│    → 배너, 메뉴카드 등 현재 메인페이지 │
│                                     │
│  ○ 하위메뉴를 홈으로 선택            │
│    ┌─────────────────────────┐      │
│    │ 아기얼굴생성      ▼      │      │
│    └─────────────────────────┘      │
│    → /baby-face 로 바로 이동          │
│                                     │
│  [ 취소 ]              [ 저장 ]      │
└─────────────────────────────────────┘
```

---

## Phase 4: 프론트엔드 — 동적 네비게이션

### BottomNavigation.tsx 변경

```typescript
// 변경 전 — 정적 배열
const navItems: NavItem[] = [
  { path: '/missions', icon: Target, label: '문화센터', ... },
  { path: '/', icon: Sparkles, label: 'AI 생성', ... },
  ...
];

// 변경 후 — DB에서 가져온 메뉴 기반
const { data: mainMenus } = useQuery({
  queryKey: ['/api/main-menus'],
  queryFn: getQueryFn({ on401: 'returnNull' }),
  staleTime: 5 * 60 * 1000, // 5분 캐시
});

const iconMap: Record<string, any> = {
  'Trophy': Trophy, 'Target': Target, 'Sparkles': Sparkles,
  'Images': Images, 'User': User,
};

const navItems = (mainMenus || fallbackItems).map(menu => ({
  path: menu.homeType === 'submenu' ? menu.homeSubmenuPath : menu.path,
  icon: iconMap[menu.icon] || Sparkles,
  label: menu.title,
  ariaLabel: `${menu.title} 페이지`,
}));
```

### MAIN_PAGE_PATHS 동적 변환

```typescript
// 변경 전
export const MAIN_PAGE_PATHS = ['/', '/missions', '/gallery', '/profile'];

// 변경 후 — 훅으로 제공
export function useMainPagePaths() {
  const { data: menus } = useQuery({ queryKey: ['/api/main-menus'], ... });
  return (menus || []).map(m => 
    m.homeType === 'submenu' ? m.homeSubmenuPath : m.path
  ).filter(Boolean);
}

// 정적 폴백 (쿼리 로딩 중 사용)
export const MAIN_PAGE_PATHS_FALLBACK = ['/', '/missions', '/gallery', '/profile'];
```

### App.tsx `getSectionTitle()` 변경

```typescript
// 변경 전 — 하드코딩
const getSectionTitle = () => {
  if (location === '/mymissions' || ...) return { title: '나의미션', href: '/mymissions' };
  if (location === '/missions' || ...) return { title: '우리병원문화센터', href: '/missions' };
  ...
};

// 변경 후 — DB 메뉴 기반
const { data: mainMenus } = useQuery({ queryKey: ['/api/main-menus'], ... });

const getSectionTitle = () => {
  if (!mainMenus) return { title: 'AI이미지생성', href: '/' }; // 폴백
  
  for (const menu of mainMenus) {
    const menuPath = menu.homeType === 'submenu' ? menu.homeSubmenuPath : menu.path;
    if (location === menuPath || location.startsWith(menuPath + '/')) {
      return { title: menu.title, href: menuPath };
    }
  }
  return { title: 'AI이미지생성', href: '/' };
};
```

---

## 상태 관리

| 용도 | 훅 | 캐시 키 |
|------|---|---------|
| 메인 메뉴 (프론트) | `useQuery` | `['/api/main-menus']` |
| 메인 메뉴 (관리자) | `useQuery` | `['/api/admin/main-menus']` |
| 메뉴 활성 토글 | `useMutation` | invalidate: `['/api/admin/main-menus']`, `['/api/main-menus']` |
| 홈 설정 저장 | `useMutation` | invalidate: `['/api/admin/main-menus']`, `['/api/main-menus']` |

---

## 에러 처리

| 상황 | HTTP 코드 | 사용자 메시지 |
|------|----------|-------------|
| 메뉴 API 로딩 실패 | — | 정적 폴백 메뉴 사용 (UX 영향 없음) |
| 중복 menuId | 409 | "이미 동일한 메뉴 ID가 존재합니다" |
| 메뉴 미존재 | 404 | "메뉴를 찾을 수 없습니다" |
| 홈 설정 오류 | 400 | "올바른 홈 설정을 입력해주세요" |

---

## 구현 순서

1. **Phase 0**: admin.tsx 컴포넌트 분리 → 동작 확인
2. **Phase 1**: DB 스키마 (`main_menus`) + 마이그레이션 + seed
3. **Phase 2**: Backend API 구현 (`admin-routes.ts`, `public-routes.ts`)
4. **Phase 3**: 관리자 UI (`MenuManagement.tsx`) + admin.tsx 탭 재편
5. **Phase 4**: `BottomNavigation.tsx` + `App.tsx` 동적 변환

## 검증 계획

### Phase 0 검증 (컴포넌트 분리)
- **명령어**: `npm run dev` → 에러 없이 구동 확인
- **브라우저 확인**: `/admin` 접속 → 모든 탭 순회하며 기존과 동일 동작 확인
  - 채팅 메뉴 탭 → 캐릭터/카테고리 표시
  - 이미지 생성 탭 → 컨셉/카테고리/스냅샷/배경제거/갤러리 표시
  - 음악 프롬프트 탭 → 스타일 목록 표시
  - 미션 시스템 탭 → 카테고리/주제미션/검수/큰미션 표시
  - 포토북 관리 탭 → 모든 서브탭 표시

### Phase 1~4 검증
- **사용자 수동 테스트**:
  1. 관리자 페이지 → 메뉴관리 탭 → 5개 메인 메뉴 표시 확인
  2. "나의미션" 활성화 → 하단바에 "나의미션" 등장 확인
  3. AI생성 홈 설정 → "하위메뉴" → "아기얼굴생성" 선택 → 하단바 "AI생성" 클릭 → `/baby-face`로 이동 확인
  4. 메뉴 순서 변경 → 하단바 순서 반영 확인
  5. 기존 모든 관리 기능 정상 작동 확인

> ✅ Design 문서 완료. 구현을 진행하시고, 완료 후 `/check 메뉴관리시스템`으로 갭 분석을 실행하세요.
