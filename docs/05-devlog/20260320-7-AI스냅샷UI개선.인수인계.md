# 20260320-7 AI스냅샷UI개선 인수인계

## 작업 요약
- **작업일시**: 2026-03-20 18:13
- **작업 유형**: UI/UX 개선 (Minor Change)
- **문제**: AI 스냅샷 생성 화면에서 선택 버튼들이 너무 커서 스크롤을 해야만 다음 단계 버튼이 보이는 등 모바일 사용성이 불편함
- **해결**: 모바일 환경에 최적화되도록 버튼 크기를 줄이고 한 화면에 더 많은 정보가 보이도록 레이아웃(그리드)을 조정함

## 변경 파일 목록

### 1. `client/src/pages/snapshot.tsx`
- **모드 선택 (개인/커플/가족)**
  - 기존: 1열 배치 (세로형)
  - 변경: 3열 배치 (가로 1줄로 나란히 보이게 수정), 모바일 패딩 축소, 설명 텍스트 모바일에서 숨김 처리
- **사진 업로드**
  - 기존: 영역이 너무 큼 (`p-12`), 불필요한 드래그 텍스트 존재
  - 변경: 상하 패딩 축소 (`p-6`), 아이콘 크기 축소, "또는 파일을 여기로 드래그하세요" 텍스트 삭제
- **스타일 선택**
  - 기존: 1열 배치 (세로형)
  - 변경: 2열 배치 (가로 2개씩 2줄로 보이게 수정), 컬러박스 높이 축소 (`h-16`), 설명 텍스트 모바일에서 숨김 처리

## 동작 확인 상태
- ✅ 코드 수정 완료 (레이아웃 관련 `className` 테일윈드 유틸리티 수정)
- ⬜ 디자인 적용 상태 라이브 테스트 필요 (현재 dev 서버 구동 중)

## 다음 작업 참고사항
- 반응형 웹(`md:` 클래스)을 적용하여, 데스크탑 화면에서는 기존과 유사하게 넓게 보이고 모바일 화면에서만 오밀조밀하게 나타납니다.
- 만약 모바일에서 텍스트가 잘리는 등 추가 조정이 필요하면 `text-sm`, `whitespace-nowrap` 부분을 세부 조정할 수 있습니다.
