# [작업명] 안드로이드 앱 아이콘 적용
작업 일시: 2026.03.20

## 변경 파일 목록
- `package.json`
- `package-lock.json`
- `assets/icon.png` (추가)
- `assets/splash.png` (추가)
- `android/app/src/main/res/mipmap-*` 내 아이콘 파일 74개 생성 및 갱신

## 핵심 변경 내용
- 첨부된 두 가지 이미지를 원본 리소스로 사용하여 안드로이드 앱 적용 준비.
- Capacitor 공식 도구인 `@capacitor/assets` 플러그인 설치 (`npm install -D`).
- 터미널 명령어(`npx capacitor-assets generate --android`)를 통해 Android 런거 아이콘(각 해상도별) 및 스플래시 스크린 자동 생성 완료.

## 동작 확인 상태 (✅/❌)
- ✅ `@capacitor/assets` 빌드 명령어 에러 없이 정상 완료 및 폴더 구조 생성 확인

## 다음 작업 참고사항
- **안드로이드 스튜디오**를 열어 프로젝트를 다시 빌드(Sync)하시거나, 터미널에서 `npm run build` 후 안드로이드 스튜디오를 실행하시면, 바탕화면(런처) 아이콘과 로딩 화면(스플래시)이 방금 올려주신 예쁜 이미지로 등록되어 있습니다.
- **구글 플레이 스토어 업로드 시 주의:** 스토어 관리자 콘솔 등록용 512x512 이미지는 별도로 구글 플레이 웹사이트에서 직접 올려주셔야 합니다 (이 파일들의 용도와는 별개입니다).
